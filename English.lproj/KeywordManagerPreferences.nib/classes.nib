{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {addCategory = id; addKeyword = id; remove = id; showHelp = id; }; 
            CLASS = KeywordManagerPreferences; 
            LANGUAGE = ObjC; 
            OUTLETS = {outlineView = NSOutlineView; prefView = NSView; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}