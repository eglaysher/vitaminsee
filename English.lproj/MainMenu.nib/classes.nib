{
    IBClasses = (
        {
            ACTIONS = {
                closeWindow = id; 
                deleteFileClicked = id; 
                goBack = id; 
                goEnclosingFolder = id; 
                goForward = id; 
                goToFolder = id; 
                goToHomeFolder = id; 
                goToPicturesFolder = id; 
                showPreferences = id; 
                toggleKeywordManager = id; 
                toggleSortManager = id; 
                toggleVitaminSee = id; 
                zoomToFit = id; 
            }; 
            CLASS = CQViewController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                currentFileViewHolder = NSView; 
                directoryDropdown = NSPopUpButton; 
                fileSizeLabel = NSTextField; 
                gotoFolderSheet = NSWindow; 
                gotoPath = NSTextField; 
                homeFolderMenuItem = NSMenuItem; 
                imageSizeLabel = NSTextField; 
                imageViewer = NSImageView; 
                mainVitaminSeeWindow = NSWindow; 
                pictureFolderMenuItem = NSMenuItem; 
                progressCurrentTask = NSTextField; 
                progressIndicator = NSProgressIndicator; 
                scrollView = NSScrollView; 
                viewAsIconsController = ViewIconViewController; 
                viewerWindow = NSWindow; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {copyButtonPushed = id; moveButtonPushed = id; }; 
            CLASS = SortManagerController; 
            LANGUAGE = ObjC; 
            OUTLETS = {mainController = CQViewController; }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {
            CLASS = ViewIconViewController; 
            LANGUAGE = ObjC; 
            OUTLETS = {controller = CQViewController; ourBrowser = NSBrowser; ourView = NSView; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}